import { MakeRedirectPipe } from './make-redirect.pipe';

describe('MakeRedirectPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeRedirectPipe();
    expect(pipe).toBeTruthy();
  });
});
